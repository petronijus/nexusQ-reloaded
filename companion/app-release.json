{
  "_comment": "Companion-app OTA manifest. The installed app fetches this from raw.githubusercontent.com (main) on demand, compares versionCode, and offers to download+install the apk when this is newer. Bump BOTH fields on every app release and upload the matching apk as the release asset named below. The app track is versioned INDEPENDENTLY of the Nexus Q image releases.",
  "version": "1.9.2",
  "versionCode": 21,
  "notes": "Fixes: download progress bar animates (throttled ~100 updates instead of ~10k setState); Nexus Q update section auto-checks on opening Settings (no longer blanks after navigating away); update-check bypasses GitHub's CDN cache so new releases show immediately; failed checks show an error instead of 'up to date'.",
  "apkUrl": "https://github.com/petronijus/nexusQ-reloaded/releases/download/app-v1.9.2/nexusq-companion-1.9.2.apk",
  "minAndroidSdk": 21
}
