<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- LAN control + mDNS discovery of the device bridge (_nexusq._tcp). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE"/>
    <!-- HCE card emulation: receive a short text from the Nexus Q reader over NFC. -->
    <uses-permission android:name="android.permission.NFC"/>
    <!-- required=false so the app still installs on phones without NFC/HCE (it is
         primarily a LAN companion); the code guards every NFC access. -->
    <uses-feature android:name="android.hardware.nfc.hce" android:required="false"/>
    <!-- BT Classic setup transport (RFCOMM provisioning). maxSdkVersion pair
         covers pre-S devices; S+ uses the runtime BLUETOOTH_SCAN/CONNECT. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <application
        android:label="nexusQ-reloaded"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- HCE service: emulates a card under AID F0010203040506 so the Nexus Q
             (acting as the NFC reader) can push a short UTF-8 text to the phone.

             android:enabled="false" — OFF BY DEFAULT, ON ONLY WHILE THE SETUP UI
             IS IN THE FOREGROUND (MainActivity toggles it in onResume/onPause).

             A DISABLED component is absent from the NFC routing table entirely,
             so a merely-installed app has zero NFC surface. That matters: this
             app broke the user's contactless payment twice, and only ever after
             a dev session (2026-07-15). A tap only ever worked with the app in
             the foreground anyway, so registering an always-live card emulation
             bought nothing and risked the phone's day job.

             The component's enabled state is PERSISTED by the platform across
             process death, which makes this fail SAFE: if we are killed
             (`pm clear`, `adb install -r`, a crash) the service stays OFF and
             payments keep working. The old approach failed the other way — it
             released NFC routing from onPause, which never runs when the process
             is killed outright. -->
        <service
            android:name=".NqHceService"
            android:enabled="false"
            android:exported="true"
            android:permission="android.permission.BIND_NFC_SERVICE">
            <intent-filter>
                <action android:name="android.nfc.cardemulation.action.HOST_APDU_SERVICE"/>
            </intent-filter>
            <meta-data
                android:name="android.nfc.cardemulation.host_apdu_service"
                android:resource="@xml/apduservice"/>
        </service>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
