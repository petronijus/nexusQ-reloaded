name: nexusq_companion
# Display name is "nexusQ-reloaded" (set per-platform: Android label, iOS/macOS
# CFBundleName/DisplayName, web title/manifest). The Dart package id stays
# nexusq_companion — Dart package names can't contain hyphens or uppercase.
description: "nexusQ-reloaded — companion app for the postmarketOS Nexus Q."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
# The app is versioned on its OWN INDEPENDENT TRACK — deliberately NOT aligned
# with the Nexus Q image/firmware releases (v1.8.2, v1.9.0, ...). App changes are
# tracked separately: an app-only fix must be shippable without implying a
# firmware release, and a firmware release must not force a fake app bump.
# Compatibility with the device is a PROTOCOL concern (see companion/PROTOCOL.md),
# not something to express by fusing version numbers.
#
# Gradle takes versionName/versionCode straight from here
# (android/app/build.gradle.kts). Bump the build number (+N) on EVERY apk handed
# to the phone: Android refuses a downgrade, and it is how we tell builds apart.
# This sat at 1.0.0+1 for dozens of builds and made "is this the fixed one?"
# unanswerable. Build with ./build-apk.sh so the UI stamp cannot drift from this.
#
# 1.1.0 — BT setup onboarding (explicit bond-then-connect, secure RFCOMM).
# 1.1.1 — NFC scoped to when a tap is actually expected (was breaking payment).
# 1.2.0 — Devices screen: BT pairing (phone + mouse/keyboard) and the desktop toggle.
# 1.3.0 — debug mode: always-on connection log, viewer behind a Developer toggle.
# 1.3.1 — Devices: background poll errors are logged, not flashed as a red bar.
version: 1.4.0+10

environment:
  sdk: ^3.12.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # mDNS / DNS-SD browsing to auto-discover the device's _nexusq._tcp bridge.
  multicast_dns: ^0.3.2

  # Plays the stock setup outro clip (assets/stock/raw/q_outro.mp4) at the
  # end of the wizard; degrades to a static fallback image when the asset is
  # absent or the plugin fails to initialize (see OutroScreen).
  video_player: ^2.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Generates the launcher icons for every platform from assets/icon/ (our own
  # glowing-ring mark — see tool/make_icon.py). Run: dart run flutter_launcher_icons
  flutter_launcher_icons: ^0.14.4

# Launcher-icon generation config (our original Nexus Q ring, not the Google "Q").
flutter_launcher_icons:
  image_path: "assets/icon/icon.png"
  android: "ic_launcher"
  min_sdk_android: 21
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/icon/icon_foreground.png"
  ios: true
  remove_alpha_ios: true
  web:
    generate: true
    image_path: "assets/icon/icon.png"
    background_color: "#000000"
    theme_color: "#33B5E5"
  macos:
    generate: true
    image_path: "assets/icon/icon.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/device/
    - assets/stock/drawable/
    - assets/stock/raw/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
