# Drop-in for systemd-fsck@dev-disk-by\x2dpartlabel-cache.service: check the
# persist filesystem only after nq-persist prepare has had its say about
# whether it IS the persist filesystem (a stock unit's Android `cache` is
# formatted there, and there is nothing to fsck before that).
[Unit]
After=nexusq-persist-prepare.service
